{
  "name": "overlay-component",
  "description": "Page overlay component",
  "version": "0.0.1",
  "dependencies": {
    "emitter-component": "*",
    "jquery-component": "*"
  },
  "component": {
    "styles": ["overlay.css"],
    "scripts": {
      "overlay": "overlay.js"
    },
    "templates": {
      "overlay": "overlay.html"
    }
  }
}
